export enum SceneType {
  INITIAL = "initial",
  EXPLORATION = "exploration",
  RESOLUTION = "resolution",
}

export enum TransitionReason {
  NEW_ANALYSIS = "new_analysis",
  USER_RECOMPOSE = "user_recompose",
  LOCK = "lock",
}
