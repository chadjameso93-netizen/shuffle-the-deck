export enum CardRoleFamily {
  CORE = "core",
  EXTERNAL = "external",
  CONTEXT = "context",
  UNKNOWN = "unknown",
}

export enum CardExpressionState {
  SHADOW = "shadow",
  DEFENDED = "defended",
  ADAPTIVE = "adaptive",
  ALIGNED = "aligned",
  GIFT = "gift",
}
